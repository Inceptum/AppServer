define([
    'Underscore',
    'Backbone'
], function (_, Backbone) {

    var messageModel = Backbone.Model.extend({
        initialize:function () {
        }
    });

    return messageModel;

});
